language: r
cache: packages
r:
    - bioc-release

os:
    - linux
    - osx

dist:
    - trusty

apt:
    sources:
      - debian-sid
    packages:
      - libfreetype6

osx_image: xcode7.3
brew_packages: cairo
latex: false

r_github_packages:
    - jimhester/covr

after_success:
    - Rscript -e 'library(covr);codecov()'

notifications:
    email: false
